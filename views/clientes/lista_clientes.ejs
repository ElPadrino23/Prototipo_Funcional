<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('./../css.ejs') %>
</head>
<body>
<%- include('./../nav.ejs') %>

<main class="app-page">
    <div class="app-header">
        <div>
            <h1 class="app-title">Clientes</h1>
            <p class="app-subtitle">Consulta de expediente, riesgo, PEP y documentos del cliente.</p>
        </div>
        <div style="display:flex; gap:8px;">
            <a href="/clientes/expediente" class="btn">Nuevo cliente</a>
            <input type="file" id="inputCsv" accept=".csv" style="display:none">
            <button class="btn" onclick="document.getElementById('inputCsv').click()">Importar desde CSV</button>
        </div>
    </div>

    <% if (typeof mensaje !== 'undefined' && mensaje) { %>
        <div class="feedback"><%= mensaje %></div>
    <% } %>

    <section class="panel">
        <table class="striped responsive-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre / Razon social</th>
                    <th>RFC</th>
                    <th>Tipo</th>
                    <th>Riesgo</th>
                    <th>PEP</th>
                    <th>Docs</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody id="clientesBody">
                <tr>
                    <td colspan="7">Cargando clientes...</td>
                </tr>
            </tbody>
        </table>
    </section>
</main>

<script src="/js/db-ajax.js"></script>
<script>
    window.addEventListener('load', function() {
        cargarClientes();
    });

    document.getElementById('inputCsv').addEventListener('change', async function() {
        const archivo = this.files[0];
        if (!archivo) return;

        const formData = new FormData();
        formData.append('csv', archivo);

        try {
            const resp   = await fetch('/clientes/importar-csv', { method: 'POST', body: formData });
            const result = await resp.json();

            if (result.exito) {
                alert('Se importaron ' + result.insertados + ' clientes correctamente');
                window.location.reload();
            } else {
                alert('Error al importar: ' + result.error);
            }
        } catch (err) {
            alert('Error: ' + err.message);
        }

        this.value = '';
    });
</script>
</body>
</html>
